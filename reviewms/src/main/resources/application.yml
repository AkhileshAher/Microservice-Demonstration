spring:
    application:
        name: reviewms
    datasource:
        url: h
        username: j
        password: h

    jpa:
        show-sql: true
        hibernate:
            ddl-auto: update
        properties:
            format_sql: true

server:
    port: 8083
